mutation AddReactionToIssue($reactionInput: AddReactionInput!) {
    addReaction(input: $reactionInput) {
        reaction {
            content
        }
        subject {
            id
        }
    }
}